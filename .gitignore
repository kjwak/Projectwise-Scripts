

# Virtual environments (any name)
.venv/
.venv_overlay_build/
.venv*/
venv/
ENV/
env/
.env
.env.*

# --- PyInstaller (keep *.spec in git for reproducible builds) ---
# workpath: tools/overlay/build/  distpath: dist/  (set in qc_overlay_prepend.spec)

db/

# Python bytecode / caches
__pycache__/
*.pyc
.pytest_cache/

# Local / test appsettings (merged at runtime; see docs/reference/testing-config.md)
appsettings.local.json
appsettings.secrets.json
appsettings.test.json
appsettings.*.local.json
!appsettings.test.json.example
!appsettings.secrets.json.example

# Laptop-only diagnostics queue/log share override (see config/qc-debug-locations.example.json)
config/qc-debug-locations.local.json

# Machine-specific Cursor MCP launch paths. Copy .cursor/mcp.json.example → .cursor/mcp.json
.cursor/mcp.json

# MCP inspector dumps / local venv. Server source under tools/pw-qc-mcp/ stays tracked.
tools/pw-qc-mcp/_*.json
tools/pw-qc-mcp/_*.txt
tools/pw-qc-mcp/.venv/
reports/
tools/overlay/build/
build/
test/output/

# Local archive of superseded repo snapshot (nested .git; not part of this project)
archive/old files/
old files/