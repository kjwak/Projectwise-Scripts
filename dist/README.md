# `dist/` (generated)

Packaged build artifacts (generated), including:

- `dist/qc_overlay_prepend/qc_overlay_prepend.exe` (used by `modules/Processing/QC.Processors.psm1` when overlay is enabled)

Avoid manually editing files under `dist/`—rebuild from sources instead.

