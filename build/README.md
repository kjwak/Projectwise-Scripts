# `build/` (generated)

Intermediate build outputs (generated).

- Safe to delete and regenerate as part of rebuild workflows.
- Source-of-truth for the overlay build configuration is `qc_overlay_prepend.spec` in the repo root.

